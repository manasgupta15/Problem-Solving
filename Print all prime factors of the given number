#python program to find the prime factor of a number...

num=int(input("enter the number n: \n"))
i=1
while(i<=num):
    count=0
    if(num%i==0):
        j=1
        while(j<=i):
            if(i%j==0):
                count=count+1
            j=j+1
        if(count==2):
            print("%d is a prime factor of given number %d"%(i,num))
    i=i+1
