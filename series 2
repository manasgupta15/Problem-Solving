def factorial(n):
    fact = 1
    sum = 0.0
    for i in range(1, n + 1):
        fact = fact * i
        sum = sum + (fact / i)
    return sum

n=int(input("enter the number n: \n"))
    
result=factorial(n)
print(result)
